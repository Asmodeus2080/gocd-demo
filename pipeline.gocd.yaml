format_version: 10
pipelines:
  basic-pipeline:
    group: test-group
    materials:
      - gitMaterial:
          git: https://github.com/Asmodeus2080/go-cd-material-rep.git
          branch: main
    stages:
      - test-stage:
          jobs:
            - name: test-job
              tasks:
                - exec:
                    command: echo
                    arguments: ["This is a pipeline with a valid material."]
